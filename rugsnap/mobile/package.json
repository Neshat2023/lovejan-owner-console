{
  "name": "rugsnap-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "RugSnap native iOS wrapper (Capacitor) for TestFlight & the App Store",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
